--계정 생성 시 즐겨찾기 자동 생성
INSERT INTO TB_SYS_FAVORITE SELECT
:USER_ID AS USER_ID,
PROGRAM_SEQ,
'' AS SORT,
'N' USE_YN,
sysdate AS INS_DT,
'' AS UPD_USER,
'' AS UPD_DT
FROM 
TB_SYS_MENU
WHERE AUTH_SEQ=:AUTH_SEQ AND ORGCD =:ORGCD;

--계정 생성 시 항목 자동 생성
INSERT INTO TB_SYS_DOMAIN_NEW 
SELECT 
	:orgcd AS ORG_CD,
	HEADERNAME,
	HEADERALIGN,
	"TYPE",
	WIDTH,
	ALIGN,
	FIELD,
	PAGE,
	SORT,
	VISIABLE,
	CATEGORY,
	:userid AS USER_ID 
FROM TB_SYS_DOMAIN_NEW 
	WHERE ORG_CD IS NULL;