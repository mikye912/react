--사업부 생성 시 검색조건 자동 생성
INSERT INTO TB_BAS_SEARCHBOX_NEW 
	SELECT 
		:ORGCD AS ORG_CD, 
		PAGE, 
		NAME, 
		FIELD, 
		TYPE, 
		DEFAULT_YN, 
		SORT 
	FROM TB_BAS_SEARCHBOX_NEW 
		WHERE org_cd IS NULL; 

--사업부 생성 시 항목 자동 생성
INSERT INTO TB_SYS_DOMAIN_NEW 
SELECT 
	:orgcd AS ORG_CD,
	HEADERNAME,
	HEADERALIGN,
	"TYPE",
	WIDTH,
	FIELD,
	PAGE,
	SORT,
	VISIABLE,
	CATEGORY 
FROM TB_SYS_DOMAIN_NEW 
	WHERE ORG_CD IS NULL;